{
  "name": "coop-saving-app",
  "private": true,
  "scripts": {
    "dev": "pnpm exec concurrently -n WEB,API -c auto \"pnpm dev:web\" \"pnpm dev:api\"",
    "start": "pnpm dev",

    "dev:web": "pnpm --filter web dev",
    "build:web": "pnpm --filter web build",
    "lint:web": "pnpm --filter web lint",

    "setup:api": "cd apps/api && uv sync",
    "dev:api": "cd apps/api && uv run uvicorn app.main:app --reload",
    "seed:api": "cd apps/api && uv run python -m app.seed"

  },
  "packageManager": "pnpm@9.12.0",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}